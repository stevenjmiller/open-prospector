# Controller qualification input

The recorded classifier result binds the first rock observation produced by
the frozen synthetic scenario, run `run:controller-test`, original directive,
3000-tick one-way channel, and the ten-tick acceptance actor. It was prepared
with the deterministic test stub, then independently consumed by the recorded
seam. Runtime never regenerates a mismatched record or calls that stub.

`npm run controller` uses this file in a real endpoint child process. A changed
observation identity, timing or content must fail the binding until explicitly
reviewed and versioned. This is a qualification record, not a learned perception
result or hardware evidence.
